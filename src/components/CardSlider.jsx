import React, { useEffect, useState } from "react";
import { ActivityIndicator, Image, ScrollView, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { collection, getDocs } from "firebase/firestore";
import { db } from "../../firebase/firebaseConfig";
import { colors, shadow } from "../design/theme";
export default function CardSlider({navigation,category}){const [products,setProducts]=useState([]);useEffect(()=>{getDocs(collection(db,"Products")).then(s=>setProducts(s.docs.map(d=>({id:d.id,...d.data()})).filter(p=>p.available!==false))).catch(e=>{console.error("CardSlider load error:",e)})},[]);const visible=products.filter(p=>!category||String(p.category||p.categoryId||"").toLowerCase()===category.toLowerCase());return <View style={styles.container}><View style={styles.headingRow}><Text style={styles.header}>Popular right now</Text><TouchableOpacity onPress={()=>navigation.navigate("AllProducts")}><Text style={styles.small}>See all</Text></TouchableOpacity></View>{products.length===0?<View style={styles.empty}><ActivityIndicator color={colors.primary}/><Text style={styles.emptyText}>Loading menu...</Text></View>:<ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.scroll}>{visible.map(p=><TouchableOpacity key={p.id} style={styles.card} activeOpacity={.9} onPress={()=>navigation.navigate("ProductScreen",{product:p})}><Image style={styles.image} source={p.imageUrl?{uri:p.imageUrl}:require("../Images/Biryani2.jpg")}/><View style={styles.body}><Text style={styles.name} numberOfLines={1}>{p.Name||p.name||"Food item"}</Text><Text style={styles.category}>{p.category||p.categoryId||"Food"}</Text><Text style={styles.price}>Rs. {Number(p.price||0)}</Text></View></TouchableOpacity>)}</ScrollView>}</View>}
const styles=StyleSheet.create({container:{marginTop:14},headingRow:{paddingHorizontal:16,flexDirection:"row",justifyContent:"space-between",alignItems:"center",marginBottom:10},header:{fontSize:20,fontWeight:"900",color:colors.text},small:{color:colors.primary,fontWeight:"800"},scroll:{paddingHorizontal:16,gap:12},empty:{padding:30,alignItems:"center"},emptyText:{color:colors.muted,marginTop:8},card:{width:205,backgroundColor:"#fff",borderRadius:20,overflow:"hidden",...shadow},image:{width:"100%",height:145,resizeMode:"cover"},body:{padding:12},name:{fontSize:16,fontWeight:"900",color:colors.text},category:{fontSize:11,color:colors.muted,marginTop:4},price:{fontSize:16,fontWeight:"900",color:colors.primary,marginTop:7}});
